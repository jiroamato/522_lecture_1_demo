# 522_lecture_1_demo

This is repo for the code along for lecture 1.

- `git branch <name>`: create a branch <name> in your current HEAD location
- `git switch <name>`: switch to the <name> branch
- `git switch -c <name>`: create <name> and switch to it in one go from local HEAD
- `git fetch --prune`: 
- `git branch -d <name>`: deleted branch locally
